name: ER6 activity integration

on:
  push:
    branches: [er6-unified-reading-activity]
    paths:
      - .github/workflows/er6-apply.yml

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: er6-unified-reading-activity
          fetch-depth: 0
      - name: Apply ER6 audit fixes
        run: python .github/scripts/er6_fix.py
      - name: Commit ER6 audit fixes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src
          git diff --cached --quiet && exit 0
          git commit -m "ER6 harden activity recency and successful-open semantics"
          git pull --rebase origin er6-unified-reading-activity
          git push origin HEAD:er6-unified-reading-activity
