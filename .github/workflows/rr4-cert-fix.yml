name: RR4 Apply Certificate Correction

on:
  push:
    branches: [release/rr4-performance-memory]
    paths:
      - 'scripts/rr4-cert-fix.mjs'
      - '.github/workflows/rr4-cert-fix.yml'

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
        with:
          ref: release/rr4-performance-memory
          fetch-depth: 0

      - name: Apply certificate correction
        run: node scripts/rr4-cert-fix.mjs

      - name: Commit non-workflow correction
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git restore --source=HEAD --staged --worktree .github/workflows/rr4-cert-fix.yml
          git diff --cached --quiet && exit 0
          git commit -m 'RR4 validate active PDF search cancellation'
          git push origin HEAD:release/rr4-performance-memory
