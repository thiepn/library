# Reader touch clientX recovery

This recovery follows Deploy Library run `35127222550` for `main` SHA `e671625fcaff727780c71440b8e631b127fefac6`. That deployment completed qualification/staging, staged browser acceptance, RR4 performance/memory, RR5 offline/storage reliability, and RR7 ergonomics, but failed closed in RR6 accessibility on the phone deep-reading tap-zone acceptance path. The affected public-domain batch remains unpublished until a later `main` SHA completes the entire deployment pipeline and exact live-production source-identity verification.

The retained RR6 trace showed a paginated EPUB iframe spanning many CSS page widths while the publication body declared one visible page width. A physical center tap therefore has an unambiguous `clientX` inside the declared visible publication page even when a browser-emulation environment also supplies a synthetic `screenX`. The previous ordering consulted physical screen geometry before that visible-page `clientX`, which could classify a center tap as an edge navigation tap and move reading location instead of toggling reader chrome.

The repair changes only coordinate precedence: when `clientX` already fits the publication's declared visible page inside a wider paginated iframe, that visible-page coordinate is accepted before physical-screen fallback. Real touch events whose `clientX` is translated into the wider strip still fall through to the existing physical-screen, parent-frame, overflow-container, and local-coordinate recovery paths. Interactive controls, selections, swipe handling, compatibility-click suppression, reader navigation thresholds, acquisition rules, copyright screening, provenance, cover generation, immutable release identities, R2 integrity checks, and all publication/release gates are unchanged.

The exact pull-request head must pass every applicable qualification workflow before merge. After merge, the resulting exact `main` SHA must pass the complete Deploy Library pipeline, GitHub Pages deployment, and exact live-production identity verification before the affected public-domain batch is considered published.
