{
  "name": "thiepn-library",
  "version": "1.0.0-rc.1",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.21.0",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "validate:content": "tsx scripts/validate-content.ts",
    "validate": "pnpm check && pnpm validate:content",
    "build:astro": "astro build",
    "index": "pagefind --site dist/library",
    "prepare:deploy": "node scripts/prepare-deploy.mjs",
    "build": "pnpm validate && pnpm build:astro && pnpm index && pnpm prepare:deploy",
    "l17b:verify": "tsx scripts/l17b-verify.ts",
    "certify:source": "node scripts/certification/source.mjs && node scripts/certification/reader-toc.mjs && node scripts/certification/reader-navigation.mjs && node scripts/certification/reader-progress.mjs && node scripts/certification/work-detail.mjs && node scripts/certification/library-home.mjs",
    "certify:postbuild": "node scripts/certification/post-build.mjs",
    "release:certify": "pnpm certify:source && pnpm l17b:verify && pnpm build && pnpm certify:postbuild",
    "stage:media": "node scripts/stage-publication-media.mjs",
    "verify:production": "node scripts/verify-production.mjs",
    "deploy": "pnpm release:certify && pnpm stage:media"
  },
  "dependencies": {
    "astro": "^6.0.0",
    "epubjs": "0.3.93",
    "yaml": "^2.8.1",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@types/node": "^22.20.1",
    "pagefind": "^1.4.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "wrangler": "^4.32.0"
  }
}
