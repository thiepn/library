---
import BaseLayout from '../layouts/BaseLayout.astro';
---
<BaseLayout title="Offline" description="Offline Library package status." noindex>
  <div class="page"><section class="section"><div class="section-heading"><div><p class="eyebrow">Local / Offline</p><h1>Offline</h1></div></div><div class="empty"><h2>No publication packages are installed.</h2><p>Offline publication controls will become available only after a real Web/PDF/Course payload is materialized and passes the explicit package-integrity gate. The recovery build does not pretend that unavailable media is cached.</p></div></section></div>
</BaseLayout>
